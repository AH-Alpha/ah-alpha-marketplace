# 🔐 متغيرات البيئة المطلوبة (Environment Variables)

هذا الملف يوثق جميع متغيرات البيئة المطلوبة لتشغيل AH Alpha Marketplace.

---

## 📋 المتغيرات الإلزامية

### 1. قاعدة البيانات (Database)

#### `DATABASE_URL`
- **الوصف:** رابط الاتصال بقاعدة بيانات MySQL
- **التنسيق:** `mysql://username:password@host:port/database`
- **مثال:** `mysql://root:mypassword@containers-us-west-123.railway.app:3306/railway`
- **مصدر:** Railway MySQL service أو أي مزود MySQL آخر

---

### 2. الأمان (Security)

#### `JWT_SECRET`
- **الوصف:** مفتاح سري لتوقيع JWT tokens (الجلسات)
- **التنسيق:** نص عشوائي طويل (32+ حرف)
- **مثال:** `ahAlpha2025SecretKey!@#$%^&*()_+RandomString123`
- **ملاحظة:** **لا تشارك هذا المفتاح أبداً!** استخدم مفتاح فريد لكل بيئة

**كيفية إنشاء مفتاح عشوائي آمن:**
```bash
# Linux/Mac
openssl rand -base64 32

# أو استخدم موقع
https://randomkeygen.com/
```

---

### 3. البريد الإلكتروني (Email - SMTP)

#### `SMTP_HOST`
- **الوصف:** عنوان خادم SMTP
- **القيمة الافتراضية:** `smtp.gmail.com`
- **بدائل:**
  - Gmail: `smtp.gmail.com`
  - Outlook: `smtp-mail.outlook.com`
  - Yahoo: `smtp.mail.yahoo.com`
  - SendGrid: `smtp.sendgrid.net`
  - Mailgun: `smtp.mailgun.org`

#### `SMTP_PORT`
- **الوصف:** منفذ خادم SMTP
- **القيمة الافتراضية:** `587`
- **الخيارات:**
  - `587` - TLS (موصى به)
  - `465` - SSL
  - `25` - غير مشفر (غير موصى به)

#### `SMTP_USER`
- **الوصف:** عنوان البريد الإلكتروني للإرسال
- **مثال:** `your-email@gmail.com`
- **ملاحظة:** يجب أن يكون بريد Gmail صالح

#### `SMTP_PASS`
- **الوصف:** كلمة مرور التطبيق (App Password)
- **التنسيق:** 16 حرف (مع أو بدون مسافات)
- **مثال:** `abcd efgh ijkl mnop`
- **ملاحظة:** **ليس كلمة مرور Gmail العادية!** يجب الحصول عليها من Google Account Security

**كيفية الحصول على App Password:**
1. اذهب إلى https://myaccount.google.com/security
2. فعّل "التحقق بخطوتين"
3. ابحث عن "App passwords"
4. أنشئ App Password جديد
5. انسخ الكود المكون من 16 حرف

#### `FROM_EMAIL`
- **الوصف:** البريد الإلكتروني الذي سيظهر كمرسل
- **القيمة الافتراضية:** نفس قيمة `SMTP_USER`
- **مثال:** `noreply@ahalpha.com`

#### `FROM_NAME`
- **الوصف:** اسم المرسل الذي سيظهر في البريد
- **القيمة الافتراضية:** `AH Alpha`
- **مثال:** `AH Alpha Marketplace`

---

### 4. التطبيق (Application)

#### `NODE_ENV`
- **الوصف:** بيئة التشغيل
- **القيم المسموحة:**
  - `development` - للتطوير المحلي
  - `production` - للإنتاج (Railway)
- **القيمة الموصى بها:** `production`

#### `PORT`
- **الوصف:** المنفذ الذي سيعمل عليه السيرفر
- **القيمة الافتراضية:** `3000`
- **ملاحظة:** Railway قد يتجاهل هذه القيمة ويستخدم منفذ ديناميكي

---

## 🎨 المتغيرات الاختيارية

### 5. العلامة التجارية (Branding)

#### `VITE_APP_TITLE`
- **الوصف:** عنوان التطبيق
- **القيمة الافتراضية:** `AH Alpha`
- **مثال:** `AH Alpha - سوق العراق الإلكتروني`

#### `VITE_APP_LOGO`
- **الوصف:** مسار شعار التطبيق
- **القيمة الافتراضية:** `/logo.png`
- **مثال:** `/assets/logo.svg`

---

### 6. التحليلات (Analytics) - اختياري

#### `VITE_ANALYTICS_ENDPOINT`
- **الوصف:** رابط API للتحليلات
- **مثال:** `https://analytics.example.com/api`
- **ملاحظة:** اتركه فارغاً إذا لم تستخدم نظام تحليلات

#### `VITE_ANALYTICS_WEBSITE_ID`
- **الوصف:** معرف الموقع في نظام التحليلات
- **مثال:** `abc123xyz`
- **ملاحظة:** اتركه فارغاً إذا لم تستخدم نظام تحليلات

---

## 📝 ملف .env للتطوير المحلي

إذا كنت تعمل على الموقع محلياً، أنشئ ملف `.env` في المجلد الرئيسي:

```env
# Database
DATABASE_URL=mysql://root:password@localhost:3306/ah_alpha

# Security
JWT_SECRET=local-development-secret-key-change-in-production

# Email (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here
FROM_EMAIL=your-email@gmail.com
FROM_NAME=AH Alpha

# Application
NODE_ENV=development
PORT=3000

# Branding
VITE_APP_TITLE=AH Alpha
VITE_APP_LOGO=/logo.png
```

**⚠️ تحذير:** لا ترفع ملف `.env` إلى GitHub أبداً!

---

## 🔒 أفضل الممارسات الأمنية

### 1. لا تشارك المفاتيح السرية
- ❌ لا تضع المفاتيح في الكود
- ❌ لا ترفع ملف `.env` إلى GitHub
- ❌ لا تشارك `JWT_SECRET` أو `SMTP_PASS` مع أحد

### 2. استخدم مفاتيح مختلفة لكل بيئة
- Development: مفتاح مختلف
- Staging: مفتاح مختلف
- Production: مفتاح مختلف

### 3. غيّر المفاتيح بشكل دوري
- غيّر `JWT_SECRET` كل 3-6 أشهر
- غيّر `SMTP_PASS` عند الشك في تسريبه

### 4. استخدم App Passwords فقط
- ❌ لا تستخدم كلمة مرور Gmail الأساسية
- ✅ استخدم App Password دائماً

---

## 🧪 اختبار المتغيرات

بعد إعداد المتغيرات، اختبر:

### 1. قاعدة البيانات
```bash
# اختبر الاتصال
pnpm db:push
```

### 2. البريد الإلكتروني
- سجل حساب جديد
- تحقق من وصول البريد الإلكتروني

### 3. JWT
- سجل دخول
- تحقق من بقاء الجلسة نشطة

---

## 📞 المساعدة

إذا واجهت مشاكل:

### مشكلة: البريد لا يُرسل
**الحل:**
1. تأكد من تفعيل التحقق بخطوتين في Google
2. تأكد من استخدام App Password (ليس كلمة المرور العادية)
3. تحقق من `SMTP_USER` و `SMTP_PASS`

### مشكلة: خطأ في قاعدة البيانات
**الحل:**
1. تحقق من صحة `DATABASE_URL`
2. تأكد من وجود قاعدة البيانات
3. جرب `pnpm db:push`

### مشكلة: الجلسات لا تعمل
**الحل:**
1. تأكد من وجود `JWT_SECRET`
2. تأكد من أنه نص عشوائي طويل
3. أعد تشغيل السيرفر

---

## ✅ قائمة التحقق

قبل النشر، تأكد من:

- [ ] `DATABASE_URL` صحيح ويعمل
- [ ] `JWT_SECRET` موجود وطويل (32+ حرف)
- [ ] `SMTP_USER` و `SMTP_PASS` صحيحان
- [ ] `NODE_ENV=production`
- [ ] جميع المتغيرات مضافة في Railway
- [ ] لا توجد مفاتيح سرية في الكود
- [ ] ملف `.env` غير مرفوع إلى GitHub

---

**آخر تحديث:** ديسمبر 2025  
**الإصدار:** 2.0 (Standalone)
