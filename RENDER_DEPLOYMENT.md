# 🚀 دليل النشر على Render.com

دليل شامل لنشر موقع AH Alpha Marketplace على Render.com مجاناً.

---

## 📋 المتطلبات

قبل البدء، تأكد من توفر:

1. ✅ حساب GitHub (مربوط بالفعل)
2. ✅ حساب Render.com (سنشرح كيفية إنشائه)
3. ✅ بريد Gmail لإرسال رموز التحقق
4. ✅ قاعدة بيانات MySQL خارجية (سنشرح الخيارات)

---

## 🎯 الخطوة 1: إنشاء حساب Render

### 1.1 التسجيل
1. اذهب إلى: https://render.com
2. انقر على **"Get Started"** أو **"Sign Up"**
3. اختر **"Sign up with GitHub"**
4. اسمح لـ Render بالوصول إلى حساب GitHub

### 1.2 التحقق من البريد الإلكتروني
1. افتح بريدك الإلكتروني
2. ابحث عن رسالة من Render
3. انقر على رابط التحقق

---

## 🗄️ الخطوة 2: إعداد قاعدة البيانات MySQL

Render لا يوفر MySQL مجاني، لكن هناك بدائل ممتازة:

### الخيار 1: PlanetScale (موصى به) ⭐

**المميزات:**
- ✅ مجاني تماماً
- ✅ 5GB تخزين
- ✅ 1 مليار قراءة شهرياً
- ✅ متوافق مع MySQL

**الخطوات:**
1. اذهب إلى: https://planetscale.com
2. سجل دخول بحساب GitHub
3. انقر **"Create a new database"**
4. اسم قاعدة البيانات: `ah_alpha_marketplace`
5. اختر Region: **US East (Ohio)** (الأقرب)
6. انقر **"Create database"**
7. انقر **"Connect"**
8. اختر **"Node.js"** من القائمة
9. انسخ `DATABASE_URL` (سنحتاجها لاحقاً)

مثال على DATABASE_URL:
```
mysql://username:password@aws.connect.psdb.cloud/ah_alpha_marketplace?ssl={"rejectUnauthorized":true}
```

### الخيار 2: Railway MySQL

إذا كنت تستخدم Railway بالفعل:
1. في Railway Dashboard
2. انقر **"+ New"** → **"Database"** → **"MySQL"**
3. انتظر حتى يتم الإنشاء
4. انقر على MySQL service
5. اذهب إلى **"Connect"**
6. انسخ `DATABASE_URL`

### الخيار 3: Aiven (مجاني)

1. اذهب إلى: https://aiven.io
2. سجل حساب جديد
3. اختر **"MySQL"**
4. اختر **"Free plan"**
5. انسخ `DATABASE_URL`

---

## 🚀 الخطوة 3: نشر الموقع على Render

### 3.1 إنشاء Web Service

1. في Render Dashboard: https://dashboard.render.com
2. انقر **"New +"** في الأعلى
3. اختر **"Web Service"**
4. اختر **"Build and deploy from a Git repository"**
5. انقر **"Next"**

### 3.2 ربط GitHub Repository

1. ابحث عن `ah-alpha-marketplace`
2. انقر **"Connect"** بجانب المشروع
3. إذا لم يظهر المشروع:
   - انقر **"Configure account"**
   - اسمح لـ Render بالوصول للمشروع
   - ارجع وحاول مرة أخرى

### 3.3 تكوين Web Service

املأ الحقول التالية:

**Name:** `ah-alpha-marketplace`

**Region:** `Oregon (US West)` (أو الأقرب لك)

**Branch:** `main`

**Root Directory:** (اتركه فارغاً)

**Runtime:** `Node`

**Build Command:**
```bash
pnpm install && pnpm build
```

**Start Command:**
```bash
pnpm start
```

**Plan:** `Free` ✅

انقر **"Advanced"** لإضافة المتغيرات البيئية

---

## 🔐 الخطوة 4: إضافة المتغيرات البيئية (Environment Variables)

في قسم **"Environment Variables"**، أضف المتغيرات التالية:

### 4.1 المتغيرات الأساسية

| Key | Value | ملاحظات |
|-----|-------|---------|
| `NODE_ENV` | `production` | ثابت |
| `DATABASE_URL` | (من PlanetScale أو Railway) | نسخته من الخطوة 2 |
| `JWT_SECRET` | (نص عشوائي طويل) | مثال: `your-super-secret-jwt-key-change-this-123456` |

### 4.2 متغيرات البريد الإلكتروني (SMTP)

للحصول على App Password من Gmail:

1. اذهب إلى: https://myaccount.google.com/security
2. فعّل **"2-Step Verification"** إذا لم يكن مفعلاً
3. ارجع إلى Security
4. ابحث عن **"App passwords"**
5. اختر **"Mail"** و **"Other (Custom name)"**
6. اكتب: `AH Alpha Marketplace`
7. انقر **"Generate"**
8. انسخ الـ 16 رقم (بدون مسافات)

الآن أضف:

| Key | Value | ملاحظات |
|-----|-------|---------|
| `SMTP_HOST` | `smtp.gmail.com` | ثابت |
| `SMTP_PORT` | `587` | ثابت |
| `SMTP_USER` | `your-email@gmail.com` | بريدك الحقيقي |
| `SMTP_PASS` | (App Password من Gmail) | 16 رقم |
| `FROM_EMAIL` | `your-email@gmail.com` | نفس البريد |
| `FROM_NAME` | `AH Alpha` | اسم المرسل |

---

## ✅ الخطوة 5: إطلاق الموقع

1. بعد إضافة جميع المتغيرات البيئية
2. انقر **"Create Web Service"** في الأسفل
3. Render سيبدأ بناء الموقع تلقائياً

### 5.1 مراقبة البناء (Build)

1. ستفتح صفحة Logs تلقائياً
2. راقب العملية:
   - `Installing dependencies...` ✅
   - `Building client...` ✅
   - `Starting server...` ✅
   - `[OAuth] Disabled - running in standalone mode` ✅
   - `Server running on http://localhost:3000/` ✅

### 5.2 المدة المتوقعة

- **Build**: 3-5 دقائق
- **Deploy**: 30 ثانية

---

## 🌐 الخطوة 6: الوصول إلى الموقع

### 6.1 الرابط الافتراضي

بعد نجاح النشر، ستحصل على رابط مثل:
```
https://ah-alpha-marketplace.onrender.com
```

### 6.2 ربط دومين مخصص (اختياري)

1. في Render Dashboard
2. اذهب إلى **"Settings"**
3. ابحث عن **"Custom Domain"**
4. انقر **"Add Custom Domain"**
5. أدخل الدومين الخاص بك
6. اتبع التعليمات لتحديث DNS

---

## 🔧 الخطوة 7: تشغيل قاعدة البيانات

### 7.1 تطبيق Schema

بعد نجاح النشر، نحتاج تطبيق database schema:

**الطريقة 1: من Render Shell**

1. في Render Dashboard
2. انقر على service الخاص بك
3. اذهب إلى **"Shell"** من القائمة الجانبية
4. انقر **"Launch Shell"**
5. اكتب:
```bash
pnpm db:push
```
6. اضغط Enter

**الطريقة 2: من جهازك المحلي**

إذا كان عندك access لقاعدة البيانات:

1. افتح terminal على جهازك
2. استنسخ المشروع:
```bash
git clone https://github.com/AH-Alpha/ah-alpha-marketplace.git
cd ah-alpha-marketplace
```
3. ثبت dependencies:
```bash
pnpm install
```
4. أنشئ ملف `.env`:
```bash
DATABASE_URL="your-database-url-here"
```
5. شغل migration:
```bash
pnpm db:push
```

---

## ✅ الخطوة 8: التحقق من عمل الموقع

### 8.1 اختبار الصفحة الرئيسية

1. افتح رابط الموقع: `https://your-app.onrender.com`
2. يجب أن تظهر الصفحة الرئيسية بدون أخطاء

### 8.2 اختبار التسجيل

1. انقر **"تسجيل"**
2. املأ البيانات
3. يجب أن يصلك بريد إلكتروني برمز التحقق
4. أدخل الرمز
5. يجب أن يتم إنشاء الحساب بنجاح

### 8.3 اختبار تسجيل الدخول

1. سجل خروج
2. انقر **"تسجيل الدخول"**
3. أدخل البريد وكلمة المرور
4. يجب أن تدخل بنجاح

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: Build Failed

**الأعراض:** Render يفشل في البناء

**الحل:**
1. تحقق من Logs
2. تأكد من وجود `pnpm` في المشروع
3. تأكد من صحة `build` script في `package.json`

### المشكلة 2: Application Error

**الأعراض:** الموقع يعرض "Application Error"

**الحل:**
1. افتح Logs في Render
2. ابحث عن الأخطاء
3. تحقق من المتغيرات البيئية:
   - `DATABASE_URL` صحيح؟
   - `JWT_SECRET` موجود؟
   - `SMTP_*` كاملة؟

### المشكلة 3: Database Connection Failed

**الأعراض:** أخطاء "Cannot connect to database"

**الحل:**
1. تحقق من `DATABASE_URL`
2. تأكد من أن قاعدة البيانات تعمل
3. في PlanetScale: تأكد من أن الـ branch هو `main`
4. جرب الاتصال من جهازك المحلي أولاً

### المشكلة 4: Email Not Sending

**الأعراض:** رموز التحقق لا تصل

**الحل:**
1. تحقق من `SMTP_USER` و `SMTP_PASS`
2. تأكد من أن App Password صحيح (16 رقم)
3. تحقق من Logs للأخطاء
4. جرب إرسال بريد تجريبي من Gmail

---

## 💰 التكاليف

### Render Free Tier

- ✅ **مجاني تماماً**
- ✅ 750 ساعة شهرياً (كافية لموقع واحد)
- ⚠️ الموقع ينام بعد 15 دقيقة من عدم النشاط
- ⚠️ يستغرق 30 ثانية للاستيقاظ

### PlanetScale Free Tier

- ✅ **مجاني تماماً**
- ✅ 5GB تخزين
- ✅ 1 مليار قراءة شهرياً
- ✅ كافي لآلاف المستخدمين

### التكلفة الإجمالية: **0 دولار شهرياً** 🎉

---

## 🚀 الترقية (اختياري)

إذا احتجت أداء أفضل لاحقاً:

### Render Starter Plan - $7/شهر
- ✅ لا ينام أبداً
- ✅ أداء أسرع
- ✅ دعم أفضل

### PlanetScale Scaler Plan - $29/شهر
- ✅ 10GB تخزين
- ✅ 100 مليار قراءة
- ✅ نسخ احتياطية تلقائية

---

## 📞 الدعم

### Render Support
- 📧 البريد: support@render.com
- 💬 Discord: https://render.com/discord
- 📚 الوثائق: https://render.com/docs

### PlanetScale Support
- 📧 البريد: support@planetscale.com
- 💬 Discord: https://planetscale.com/discord
- 📚 الوثائق: https://planetscale.com/docs

---

## ✅ Checklist النشر

قبل إطلاق الموقع للجمهور، تحقق من:

- [ ] الموقع يفتح بدون أخطاء
- [ ] التسجيل يعمل
- [ ] تسجيل الدخول يعمل
- [ ] رموز التحقق تصل بالبريد
- [ ] المزادات تظهر بشكل صحيح
- [ ] السلة تعمل
- [ ] الطلبات تُنشأ بنجاح
- [ ] الصور تُرفع بشكل صحيح
- [ ] البحث يعمل
- [ ] الموقع responsive على الموبايل

---

## 🎉 تهانينا!

موقعك الآن منشور ويعمل على Render.com مجاناً! 🚀

**رابط الموقع:** `https://your-app.onrender.com`

---

## 📝 ملاحظات مهمة

1. **النوم التلقائي:** الموقع ينام بعد 15 دقيقة من عدم النشاط (Free tier)
2. **الاستيقاظ:** أول زائر بعد النوم سينتظر 30 ثانية
3. **النسخ الاحتياطية:** احفظ نسخة من قاعدة البيانات بانتظام
4. **التحديثات:** أي push إلى GitHub سيُحدّث الموقع تلقائياً

---

**بالتوفيق! 🎊**
